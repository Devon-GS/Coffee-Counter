Simple coffee counter - calculates how many of each coffee sold

Install environment:

```python
python -m venv <name>
```

Install poetry:

```python
pip install poetry
``` 

Install dependencies:

```python
poetry install
```

Run Script:

```python
python main.py
``` 